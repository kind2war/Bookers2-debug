<%= form_with model: [book, book_comment], local: true do |f| %>
  <div class="form-group">
    <% f.label :comment %>
    <%= f.text_area :comment, placeholder: "コメントをここに", class: 'form-control book_comment' %>
    <%= f.submit "送信", class: 'btn btn-success' %>
  </div>
<% end %>
