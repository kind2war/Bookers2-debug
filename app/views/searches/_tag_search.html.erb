<% if user_signed_in? %>
  <div class="tag_search_form">
    <%= form_with url: tag_search_path, local: true, method: :get do |f| %>
      <%= f.text_field :tag %>
      <%= f.submit "ﾀｸﾞ検索", class: "btn btn-primary" %>
    <% end %>
  </div>
<% end %>